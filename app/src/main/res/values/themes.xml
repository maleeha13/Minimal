<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Minimal" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->

        <!-- Add or modify attributes here -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorButtonNormal">@color/black</item> <!-- Custom background color for buttons -->
        <item name="android:windowBackground">@color/black</item>

    </style>

    <style name="buttonTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorButtonNormal">@color/black</item>


    </style>

    <style name="Button.RoundCorner" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/rounded</item>
    </style>

    <style name="Theme.Minimal" parent="Theme.AppCompat.DayNight.NoActionBar" />
</resources>
